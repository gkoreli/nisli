/**
 * Reserved public seam for the @nisli/router Vite adapter.
 *
 * ROUTER-1 publishes the subpath contract; the adapter implementation lands
 * with the Vite integration milestone so it can consume ApplicationRouter's
 * environment-neutral definition and matcher without duplicating route rules.
 */
export {};
