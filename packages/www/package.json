{
  "name": "@nisli/www",
  "version": "0.0.0",
  "private": true,
  "description": "The nisli website \u2014 home, docs, design showcase, and component gallery, rendered static by @nisli/ssg.",
  "type": "module",
  "scripts": {
    "sync": "pnpm --filter @nisli/ui build && node scripts/sync.mjs",
    "render": "vitest run src/render.test.ts",
    "build": "pnpm render && tailwindcss -i src/styles/input.css -o dist/assets/site.css --minify",
    "deploy": "pnpm build && npx wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@nisli/core": "workspace:*",
    "@nisli/ssg": "workspace:*",
    "@nisli/ui": "workspace:*",
    "@tailwindcss/cli": "^4.0.0"
  }
}
